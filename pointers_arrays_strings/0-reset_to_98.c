/**
 * main - check the code 
 *
 * Return: Always 0.
 */
int main(void)
{
  void updateValue(int*ptr) {
    *ptr = 98;
  } 

  int() {
    int number = 42
      int *ptr = &number;
    int n;

    printf("Original value: %d\n", *ptr);
    pdateValue(ptr);
    printf("Updated value: %d\n", *ptr);

    n = 402;
    printf("n=%d\n", n);
    reset_to_98(&n);
    printf("n=%d\n", n);
    return (0);
}
